Escaped `\$` signs are literal currency and must not balance a real, unescaped delimiter — two escaped dollars plus one lone `$` is still an odd count.

Tickets cost \$5 and \$10, so find the value $n of the prize.
