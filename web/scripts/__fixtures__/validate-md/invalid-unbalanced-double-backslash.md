A double backslash before a dollar is a literal backslash followed by a real delimiter (`\\$`), not an escape — so it counts toward parity, here leaving an odd total.

The expression \\$x + 1$ is fine, but $y is missing its closer.
