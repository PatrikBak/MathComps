An escaped `\$` is a literal currency sign, not a math delimiter — it must not count toward delimiter parity, and real math alongside it must still validate.

The entry fee is \$5 and the prize is \$100. Prove that $x > 0$ holds for every solution $x$.
