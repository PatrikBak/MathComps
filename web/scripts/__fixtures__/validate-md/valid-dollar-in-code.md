A `$` inside an inline code span or a fenced block is literal text, not a math delimiter — it must be excluded from delimiter parity while real math still validates.

Set it with `export PATH=$HOME/bin`, then run:

```sh
echo "$USER owes $5"
```

Finally, prove that $n \ge 1$.
