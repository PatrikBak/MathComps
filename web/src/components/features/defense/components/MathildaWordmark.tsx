/**
 * Her name in two tones, splitting where the site wordmark splits between "Math" and "Comps". The name
 * is the same in every language, so it is written here rather than translated.
 */
export function MathildaWordmark() {
  return (
    <>
      <span className="text-brand-light">Math</span>ilda
    </>
  )
}
