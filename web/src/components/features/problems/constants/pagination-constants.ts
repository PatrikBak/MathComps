/**
 * How many problems at once will we fetch.
 */
export const DEFAULT_PAGE_SIZE = 10

/**
 * When the visible range end index is within this many items from the end,
 * trigger a prefetch of the next page (used in problem infinite scroll)
 */
export const PREFETCH_THRESHOLD = 5
