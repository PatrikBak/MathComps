/**
 * Pagination constants for consistent data loading behavior.
 */
export const DEFAULT_PAGE_SIZE = 20

/**
 * When the visible range end index is within this many items from the end,
 * trigger a prefetch of the next page.
 */
export const PREFETCH_THRESHOLD = 15
