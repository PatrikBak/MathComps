*.log

# Claude Code runtime lock files (scheduled tasks, etc.)
.claude/*.lock

# Playwright MCP scratch — screenshots, and a session injector holding live auth cookies
.playwright-mcp/

# Local secrets sitting at the root, which web/ ignores its own copy of
.env.local

# Python caches (from .claude tooling scripts, ruff, pytest)
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/