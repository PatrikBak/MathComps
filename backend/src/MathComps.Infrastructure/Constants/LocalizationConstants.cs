namespace MathComps.Infrastructure.Constants;

/// <summary>
/// Constants for localization resource paths.
/// These paths are relative to the application base directory.
/// </summary>
public static class LocalizationConstants
{
    /// <summary>
    /// Base path for metadata JSON files (without locale suffix or extension).
    /// Files are named like metadata.sk.json, metadata.en.json.
    /// </summary>
    public const string MetadataDirectory = "Resources";
}
